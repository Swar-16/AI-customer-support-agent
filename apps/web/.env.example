# Public configuration only. Never put secrets in VITE_* variables.
# API origin only: do not append /v1.
# Use the actual port on which your local FastAPI server runs.
VITE_API_BASE_URL=http://localhost:8000

# For production served behind the same origin, use:
# VITE_API_BASE_URL=